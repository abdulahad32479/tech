namespace BlockchainConstants {
  export const Ipfs = {
    // Default Infura IPFS endpoint or public gateway
    ADDRESS: "https://ipfs.infura.io:5001", 
    PATH: "https://ipfs.io/ipfs",
  };
}

export default BlockchainConstants;
